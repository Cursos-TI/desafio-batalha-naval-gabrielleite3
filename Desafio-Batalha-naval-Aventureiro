#include <stdio.h>

int main(){
    
    controle = 1;

    for (cont = 1; cont <=20; cont++)
    {
       scanf("%d", &num);
       if (conterole == 1)
       {
        menor =  num;
       }else{
        if (num < menor)
        {
           menor = num;
           controle ++;
        }
        
       }
       
    }
